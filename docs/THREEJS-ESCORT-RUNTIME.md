# Three.js escort runtime

The 3D preview now has a runtime seam for the deterministic escort combat rules:

- Card deployment creates a visible lane entity and a matching combat unit.
- Combat state advances independently from rendering.
- A Dou victory/defeat becomes a visible result overlay.
- The 3D shell remains a preview until model assets and full event presentation are complete.
